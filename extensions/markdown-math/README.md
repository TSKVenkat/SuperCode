# Markdown Math

**Notice:** This extension is bundled with SuperCode. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to SuperCode's built-in markdown preview and markdown cells in notebooks.
